# 📖 คู่มือเจาะลึกโปรเจกต์ Fitness (ฉบับอธิบายละเอียดทุกส่วน)

ไฟล์นี้สรุปการทำงานของทุกส่วนในโปรเจกต์ เพื่อให้คุณเข้าใจ Logic ทั้งหมดและตอบคำถามอาจารย์ได้อย่างแม่นยำครับ

---

## 🏗️ ส่วนที่ 1: ภาพรวมระบบ (System Overview)
ระบบของเราคือ **Fitness Booking System** ที่ใช้แบ่งหน้าที่กันชัดเจน:
*   **Frontend**: React + TypeScript (แสดงผล)
*   **Backend**: Node.js + Express + Prisma (จัดการข้อมูล)
*   **Database**: PostgreSQL (เก็บข้อมูล)

---

## �️ ส่วนที่ 2: เจาะลึกระบบฐานข้อมูล (Database)
เราใช้ **Prisma ORM** เป็นตัวช่วยคุยกับฐานข้อมูล ทำให้โค้ดสะอาดและจัดการความสัมพันธ์ของข้อมูลได้ง่าย
*   **User**: เก็บข้อมูลสมาชิกและแอดมิน (มีการ Hash รหัสผ่านเพื่อความปลอดภัย)
*   **Class**: ข้อมูลคลาสเรียน (ชื่อ, เวลา, ความจุ)
*   **Booking**: ตารางเชื่อมกลางระหว่าง User กับ Class (ใครจองคลาสไหน)
*   **Package**: เก็บแพ็กเกจที่ร้านมีให้เลือก
*   **MemberPackage**: เก็บว่าสมาชิกคนไหนซื้อแพ็กเกจอะไรและหมดอายุเมื่อไหร่
*   **Trainer**: เก็บข้อมูลผู้สอน

---

## 🔙 ส่วนที่ 3: เจาะลึก Backend Routes (อธิบายละเอียดรายหน้า)

ส่วนนี้คือหัวใจของ Logic ระบบ ผมจะอธิบายการทำงานของแต่ละไฟล์ใน `Backend/routes/` ครับ

### 1️⃣ auth.tsx (ระบบสมาชิกและการยืนยันตัวตน)
ไฟล์นี้จัดการเรื่องความปลอดภัยและการเข้าถึงระบบ
*   **Register**: รับข้อมูล -> เช็คว่าอีเมลซ้ำไหม -> บันทึกลงตาราง `User` (กำหนด Role เป็น `MEMBER` โดยค่าเริ่มต้น)
*   **Login**: เช็คอีเมล -> เช็ครหัสผ่าน -> ถ้าผ่านจะสร้าง **JWT Token** (เปรียบเสมือนบัตรผ่าน) ส่งกลับไปให้หน้าบ้าน
*   **Session**: ใช้เช็คว่า User ที่ Login อยู่ยังเป็นตัวจริงไหม โดยการแกะรหัสจาก Token
*   **Profile**: ให้ User สามารถอัปเดตชื่อหรือรหัสผ่านของตัวเองได้
*   **Admin get users**: ให้แอดมินดูรายชื่อสมาชิกทั้งหมด (ต้องเป็น Admin เท่านั้นถึงจะเข้าหน้านี้ได้)

### 2️⃣ bookings.tsx (ระบบการจองคลาส - ไฟล์ที่ยากที่สุด)
ไฟล์นี้มี Logic การตรวจสอบที่เข้มงวดที่สุด:
*   **Create Booking**: 
    1.  เช็คว่า User มีแพ็กเกจที่ยังไม่หมดอายุไหม (ถ้าไม่มีจองไม่ได้)
    2.  ใช้ระบบ **Transaction**: เป็นคำสั่งแบบ "มัดรวม" เพื่อเช็คว่าคลาสเต็มหรือยัง และบันทึกการจองไปพร้อมกัน ถ้าคลาสเต็มในเสี้ยววินาทีนั้น ระบบจะยกเลิกการทำงานทั้งหมดทันทีเพื่อป้องกัน "ที่นั่งเกิน"
    3.  เช็คว่าเคยจองคลาสนี้ไปแล้วหรือยัง (ป้องกันการจองซ้ำ)
*   **Get Bookings**: ดึงข้อมูลการจองเฉพาะของตัวเองมาแสดง (ใช้ `userId` จาก Token)
*   **Cancel Booking**: ลบการจองทิ้ง (เชื่อมโยงกับสิทธิ์เฉพาะเจ้าของการจองเท่านั้น)

### 3️⃣ classes.tsx (ระบบจัดการคลาสเรียน)
*   **Get All**: ดึงทุกคลาสมาโชว์ที่หน้าเว็บ โดยดึงข้อมูลเทรนเนอร์ที่สอนคลาสนั้นมาด้วย
*   **Create Class (Admin)**: แอดมินสร้างคลาสใหม่ โดยต้องระบุชื่อ, จำนวนที่รับ, และเวลา (ต้องเป็นเวลาในอนาคตเท่านั้น)
*   **Toggle Status**: แอดมินสามารถ "ปิด" หรือ "เปิด" คลาสได้ชั่วคราว (เช่น คลาสงดสอน)
*   **Delete**: ลบคลาสออกจากระบบ

### 4️⃣ packages.tsx (ระบบซื้อแพ็กเกจ)
*   **Get Active**: ดึงรายการแพ็กเกจที่แอดมินเปิดขายอยู่มาโชว์
*   **Subscribe**: 
    1.  เช็คว่าแพ็กเกจมีอยู่จริงไหม
    2.  เช็คว่า User มีแพ็กเกจเดิมที่ยังไม่หมดอายุไหม (ถ้ามีต้องรอให้หมดก่อนถึงจะซื้อใหม่ได้)
    3.  คำนวณวันหมดอายุอัตโนมัติ (วันปัจจุบัน + จำนวนวันที่แพ็กเกจระบุ)
*   **History**: ดูประวัติการซื้อแพ็กเกจของตัวเอง

### 5️⃣ trainers.tsx (ระบบจัดการเทรนเนอร์)
*   เป็นไฟล์ที่จัดการข้อมูลชื่อ, ความเชี่ยวชาญ และประวัติของเทรนเนอร์ เพื่อให้นำไปแสดงผลในหน้าคลาสเรียน หรือหน้าเทรนเนอร์

---

## 🎨 ส่วนที่ 4: เจาะลึก Frontend (หน้าบ้าน)

เน้นการรับส่งข้อมูลและความสวยงาม
*   **AuthContext**: ไฟล์ที่คอยบอกทุกหน้าในเว็บว่า "ตอนนี้ใครกำลังใช้งานอยู่"
*   **AdminPage**: ศูนย์รวมการควบคุมของแอดมิน (เรียกใช้ API หลายตัวพร้อมกันเพื่อแสดงผลสถิติและจัดการข้อมูล)
*   **api/client.tsx**: ตัวช่วยส่งข้อมูลที่แอบใส่ Token ไปในหัวจดหมาย (Header) อัตโนมัติทุกครั้งที่คุยกับหลังบ้าน

---

## 🔍 ส่วนที่ 6: เจาะลึกโค้ด Backend (บรรทัดต่อบรรทัด)

ส่วนนี้จะนำโค้ดจากหน้างานจริงมาอธิบายความหมายของแต่ละบรรทัดครับ

### 1️⃣ ไฟล์ `Backend/routes/auth.tsx` (ตัวอย่างส่วนการ Login)
```typescript
router.post('/login', async (req, res) => { // รับ URL /login แบบ POST
    try {
        const { email, password } = req.body; // 1. แตกข้อมูล email, password ออกจากสิ่งที่หน้าบ้านส่งมา

        const user = await prisma.user.findUnique({ // 2. สั่งให้ Prisma ไปเช็คในตาราง User ว่ามี email นี้ไหม
            where: { email }
        });

        if (!user) { // 3. ถ้าไม่เจอ User ในฐานข้อมูล
            return res.status(401).json({ error: 'Invalid credentials' }); // ตอบกลับว่า "ข้อมูลไม่ถูกต้อง"
        }

        if (user.password !== password) { // 4. ถ้าเจอ User แต่รหัสผ่านที่พิมพ์มา ไม่ตรงกับในฐานข้อมูล
            return res.status(401).json({ error: 'Invalid credentials' }); // ตอบกลับว่า "ข้อมูลไม่ถูกต้อง"
        }

        const token = jwt.sign( // 5. ถ้าผ่านหมด ให้สร้าง "บัตรผ่าน" (Token)
            {
                id: user.id.toString(), // ฝัง ID ผู้ใช้ลงในบัตร
                email: user.email,      // ฝังอีเมล
                role: user.role         // ฝังระดับสิทธิ์ (Member/Admin)
            },
            process.env.JWT_SECRET as string, // ใช้รหัสลับของ Server ในการล็อคบัตรนี้
            { expiresIn: '7d' } // กำหนดให้บัตรนี้ใช้ได้ 7 วัน
        );

        return res.json({ // 6. ส่งบัตรผ่านและข้อมูลผู้ใช้กลับไปให้หน้าบ้าน
            token,
            user: { id: user.id, name: user.name, email: user.email, role: user.role }
        });
    } catch (error) { ... }
});
```

### 2️⃣ ไฟล์ `Backend/routes/bookings.tsx` (ตัวอย่างการจองคลาส)
```typescript
router.post('/', authenticateToken, async (req: any, res) => { // ใช้ authenticateToken เพื่อเช็คว่า Login หรือยัง
    try {
        const { classId } = req.body; // รับ ID คลาสที่ผู้ใช้กดเลือก
        const userId = parseInt(req.user.id); // ดึง ID ของผู้ใช้คนนั้นออกมาจาก Token

        await prisma.$transaction(async (tx) => { // เริ่มระบบ Transaction (ต้องสำเร็จทุกขั้นตอนพร้อมกัน)
            const fitnessClass = await tx.class.findUnique({ // 1. ดึงข้อมูลคลาสและนับจำนวนคนจองปัจจุบัน
                where: { id: parseInt(classId) },
                include: { _count: { select: { bookings: true } } }
            });

            if (!fitnessClass || !fitnessClass.isActive) { // 2. เช็คว่าคลาสมีอยู่จริงและยังเปิดให้จองไหม
                throw new Error('Class not found or inactive');
            }

            if (fitnessClass._count.bookings >= fitnessClass.capacity) { // 3. เช็คว่า "เต็มหรือยัง" โดยเทียบคนจองกับความจุ
                throw new Error('Class is full');
            }

            await tx.booking.create({ // 4. ถ้าผ่านทุกเงื่อนไข ให้กดบันทึกการจองลงในตาราง
                data: {
                    userId,
                    classId: parseInt(classId)
                }
            });
        });

        return res.json({ message: 'Booked successfully' }); // ตอบกลับว่า "จองสำเร็จแล้ว"
    } catch (error: any) { ... }
});
```

### 3️⃣ ไฟล์ `Backend/routes/packages.tsx` (ตัวอย่างการซื้อแพ็กเกจ)
```typescript
router.post('/subscribe', authenticateToken, async (req: any, res) => {
    const { packageId } = req.body; // รับ ID แพ็กเกจที่ผู้ใช้เลือกซื้อ
    const userId = parseInt(req.user.id);

    const pkg = await prisma.package.findUnique({ // 1. ไปดูว่าแพ็กเกจนี้ราคาเท่าไหร่และใช้ได้กี่วัน
        where: { id: parseInt(packageId) }
    });

    const startDate = new Date(); // วันที่เริ่ม คือ "ตอนนี้"
    const endDate = new Date(); // วันที่สิ้นสุด
    endDate.setDate(endDate.getDate() + pkg.duration); // คำนวณวันสิ้นสุดโดยเอา วันนี้ + จำนวนวันที่แพ็กเกจกำหนด

    const memberPackage = await prisma.memberPackage.create({ // 2. บันทึกประวัติการซื้อแพ็กเกจลงตาราง
        data: {
            userId,
            packageId: pkg.id,
            name: pkg.name,
            price: pkg.price,
            startDate,
            endDate
        }
    });

    return res.json(memberPackage); // ส่งข้อมูลที่สมัครสำเร็จกลับไปโชว์ที่หน้าบ้าน
});
```

### 4️⃣ ไฟล์ `Backend/routes/classes.tsx` (จัดการคลาสเรียน)
```typescript
router.get('/', async (req, res) => { // ดึงข้อมูลคลาสทั้งหมด
    const classes = await prisma.class.findMany({ // 1. สั่ง Prisma ไปหาข้อมูลจากตาราง class หลายๆ แถว
        include: { 
            trainer: true, // 2. ให้ไปดึงข้อมูล Trainer ที่สอนคลาสนั้นมาด้วย (Relationship)
            _count: { select: { bookings: true } } // 3. นับจำนวนการจองปัจจุบันของคลาสนั้นๆ
        },
        orderBy: { schedule: 'asc' } // 4. เรียงตามวันเวลาที่เริ่มเรียน (ใกล้ที่สุดขึ้นก่อน)
    });
    return res.json(classes); // ส่งรายการคลาสทั้งหมดกลับไป
});

router.post('/', authenticateToken, requireAdmin, async (req, res) => { // เฉพาะ Admin เท่านั้นที่สร้างคลาสได้
    const { name, capacity, schedule, description, trainerId } = req.body; // รับข้อมูลคลาสใหม่

    const newClass = await prisma.class.create({ // 1. สั่งให้ Prisma สร้างบันทึกใหม่ลงในฐานข้อมูล
        data: {
            name,
            capacity: Number(capacity), // แปลงเป็นตัวเลข
            schedule: new Date(schedule), // แปลงเป็นรูปแบบวันที่
            description,
            trainerId: trainerId ? Number(trainerId) : null // เชื่อมโยงกับ Trainer (ถ้ามี)
        }
    });
    return res.json(newClass); // บอกหน้าบ้านว่าสร้างคลาสสำเร็จแล้ว
});
```

### 5️⃣ ไฟล์ `Backend/routes/trainers.tsx` (จัดการเทรนเนอร์)
```typescript
router.get('/all', async (req, res) => { // ดึงรายชื่อเทรนเนอร์ทั้งหมด
    const trainers = await prisma.trainer.findMany({ // 1. หาข้อมูลในตาราง trainer
        orderBy: { createdAt: 'desc' } // เรียงตามคนที่เพิ่งเพิ่มเข้ามาล่าสุด
    });
    res.json(trainers); // ส่งข้อมูลเทรนเนอร์กลับไปโชว์ที่หน้าเว็บ
});

router.post('/create', async (req, res) => { // เพิ่มเทรนเนอร์ใหม่
    const { name, specialty, bio } = req.body; // รับชื่อ, ความเชี่ยวชาญ และประวัติ
    const trainer = await prisma.trainer.create({ // 1. สร้าง Record ใหม่ในตาราง trainer
        data: { 
            name, 
            specialty, 
            bio: bio || null, 
            imageUrl: null // ตั้งตอนเริ่มเป็น null ไว้ก่อน
        }
    });
    res.json(trainer); // ส่งข้อมูลที่สร้างเสร็จกลับไป
});
```

---

## ⚙️ ส่วนที่ 7: โครงสร้างหลัก (Core Infrastructure)

นอกจากส่วนของข้อมูลแล้ว ยังมีไฟล์ที่เปรียบเสมือน "รากฐาน" ของระบบ ดังนี้ครับ

### 1️⃣ ไฟล์ `Backend/server.tsx` (หัวใจสำคัญ)
```typescript
import 'dotenv/config'; // โหลดค่าความลับจากไฟล์ .env (เช่น DB URL, JWT Secret)
import express from 'express'; // นำ Express มาสร้าง Server
import cors from 'cors'; // นำ CORS มาเปิดทางให้หน้าเว็บคุยกับหลังบ้านได้

const app = express();
const PORT = process.env.PORT || 5001; // กำหนด Port ที่ 5001

app.use(cors({ origin: 'http://localhost:3000', credentials: true })); // อนุญาตให้ React (3000) คุยด้วย
app.use(express.json()); // อนุญาตให้ Server อ่านข้อมูลแบบ JSON ได้

// การเชื่อมต่อ Route ทั้งหมด
app.use('/api/auth', authRoutes);
app.use('/api/bookings', bookingsRoutes);
// ... และไฟล์ Route อื่นๆ เพื่อแบ่งหน้าที่ให้เป็นระเบียบ ...

app.listen(PORT, () => { // สั่งให้ Server เริ่มทำงาน
    console.log(`🚀 Server started on port ${PORT}`);
});
```

### 2️⃣ ไฟล์ `Backend/middleware/auth.tsx` (ตำรวจคุมความปลอดภัย)
```typescript
export function authenticateToken(req: any, res: any, next: any) {
    const authHeader = req.headers['authorization']; // ดึง "หัวจดหมาย" ที่มี Token ออกมา
    const token = authHeader && authHeader.split(' ')[1]; // แยกเอาแค่ตัวรหัส Token

    if (!token) return res.status(401).json({ error: 'Unauthorized' }); // ถ้าไม่มีบัตรมาแสดง ห้ามเข้า

    jwt.verify(token, process.env.JWT_SECRET as string, (err, user) => { // ตรวจสอบรหัสลับในบัตร
        if (err) return res.status(403).json({ error: 'Invalid token' }); // ถ้าบัตรปลอมหรือหมดอายุ ห้ามเข้า
        
        req.user = user; // 📍 เก็บข้อมูลเจ้าของบัตรไว้ใน req.user เพื่อเอาไปใช้ในขั้นตอนถัดไป
        next(); // ผ่านด่านได้! ไปทำงานที่ Route ต่อไป
    });
}

export function requireAdmin(req: any, res: any, next: any) {
    if (!req.user || (req.user.role !== 'ADMIN')) { // เช็คระดับสิทธิ์ในบัตรว่าเป็น ADMIN ไหม
        return res.status(403).json({ error: 'Admin only' }); // ถ้าไม่ใช่ แต่อยากทำหน้าที่ Admin ห้ามเข้า
    }
    next(); // ผ่าน!
}
```

### 3️⃣ ไฟล์ `Backend/lib/prisma.tsx` (ตัวเชื่อมฐานข้อมูล)
```typescript
import { PrismaClient } from '@prisma/client';

const globalForPrisma = global as unknown as { prisma: PrismaClient };

// สร้าง Prisma Client ขึ้นมาครั้งเดียว (Singleton) เพื่อไม่ให้เชื่อมต่อ DB เยอะเกินไปจนล่ม
export const prisma = globalForPrisma.prisma || new PrismaClient();

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;
```

---

## 💻 ส่วนที่ 8: โครงสร้างหน้าบ้านระดับสูง (Advanced Frontend Core)

ส่วนนี้คือหัวใจของการเชื่อมต่อข้อมูลและการจัดการสถานะของผู้ใช้ในหน้าบ้านครับ

### 1️⃣ ไฟล์ `Frontend/src/api/client.tsx` (ตัวกลางเรียก API)
```typescript
const apiClient = axios.create({ baseURL: 'http://localhost:5001/api' });

// 🛡️ Interceptors: ส่วนที่ทำหน้าตรวจสอบทุกการรับส่งแบบอัตโนมัติ
apiClient.interceptors.request.use((config) => {
    const token = localStorage.getItem('token'); // 1. ไปหยิบบัตรผ่าน (Token) จากความจำ Browser
    if (token) {
        config.headers.Authorization = `Bearer ${token}`; // 2. แปะบัตรผ่านไปในหัวจดหมายทุกครั้งที่คุยกับหลังบ้าน
    }
    return config;
});

apiClient.interceptors.response.use( // 🛡️ ส่วนที่เช็คผลตอบกลับจากหลังบ้าน
    (response) => response,
    (error) => {
        if (error.response?.status === 401) { // 3. ถ้าหลังบ้านบอกว่า "บัตรหมดอายุ" (401)
            localStorage.removeItem('token'); // ล้างข้อมูลทิ้ง
            window.location.href = '/login'; // ส่งผู้ใช้กลับไปหน้า Login ทันที
        }
        return Promise.reject(error);
    }
);

// 📦 API Objects: แยกกลุ่มคำสั่งตามหน้างาน ทำให้โค้ดเป็นระเบียบ
export const authAPI = { login: (data) => apiClient.post('/auth/login', data) };
export const bookingsAPI = { create: (classId) => apiClient.post('/bookings', { classId }) };
```

### 2️⃣ ไฟล์ `Frontend/src/context/AuthContext.tsx` (คลังข้อมูลสถานะ)
```typescript
export const AuthProvider = ({ children }) => { // 1. ตัวล้อมแอป เพื่อให้ทุกหน้าเข้าถึงข้อมูลชุดนี้ได้
    const [user, setUser] = useState(null); // เก็บข้อมูลผู้ใช้ที่กำลัง Login (ถ้าเป็น null คือไม่ได้ Login)

    useEffect(() => { // 2. ทำงานครั้งแรกตอนเปิดเว็บ: เช็คว่าในความจำ Browser มี User ค้างอยู่ไหม
        const savedUser = localStorage.getItem('user');
        if (savedUser) setUser(JSON.parse(savedUser)); // ถ้ามี ให้เอามาใส่ในสถานะแอป
    }, []);

    const login = async (email, password) => { // 3. ฟังก์ชัน Login กลาง
        const response = await authAPI.login({ email, password });
        const { token, user } = response.data;
        
        localStorage.setItem('token', token); // เก็บ Token ลงความจำ
        localStorage.setItem('user', JSON.stringify(user)); // เก็บ User ลงความจำ
        setUser(user); // อัปเดตสถานะในแอปให้ทุกหน้าเห็น
    };

    const logout = () => { // 4. ฟังก์ชัน Logout กลาง: ล้างข้อมูลทุกอย่าง
        localStorage.removeItem('token');
        localStorage.removeItem('user');
        setUser(null);
    };

    return <AuthContext.Provider value={{ user, login, logout }}>{children}</AuthContext.Provider>;
};

export const useAuth = () => useContext(AuthContext); // 5. Hook สำหรับเรียกใช้งานง่ายๆ เช่น const { user } = useAuth();
```

---

## 🛠️ ส่วนที่ 9: สิ่งที่ควรทำความเข้าใจเพิ่มเติม (Configuration & Types)

### 1️⃣ ไฟล์ `.env` (ความลับของระบบ)
ปกติไฟล์นี้จะไม่ถูกแชร์ให้ใคร เพราะเก็บข้อมูลสำคัญ:
*   `DATABASE_URL`: ที่อยู่ของฐานข้อมูล (ถ้าเปลี่ยน DB ต้องมาแก้ตรงนี้)
*   `JWT_SECRET`: รหัสลับที่ใช้ "เซ็นชื่อ" ในบัตรผ่าน (Token) ถ้าใครรู้รหัสนี้ จะสามารถปลอมบัตรผ่านได้
*   `PORT`: กำหนดว่า Server จะทำงานที่เลขอะไร (เราตั้งไว้ 5001)

### 2️⃣ ไฟล์ `Frontend/src/types.ts` (มาตราฐานข้อมูล)
การใช้ TypeScript ทำให้เราต้องกำหนด "หน้าตา" ของข้อมูลให้ชัดเจน:
*   **Interfaces**: เป็นการบอกว่า Object แต่ละอย่างมี Field อะไรบ้าง (เช่น `User` ต้องมี `email` และ `role`)
*   **ประโยชน์**: เวลาเราเขียนโค้ดแล้วพิมพ์ชื่อ Field ผิด โปรแกรมจะแจ้งเตือนเราทันที ช่วยลด Bug ได้มหาศาลครับ

---

## 🛠️ ส่วนที่ 11: คำศัพท์และเทคโนโลยีที่ควรรู้ (Tech Stack)

เพื่อให้คุณคุยกับอาจารย์ได้ลึกซึ้งยิ่งขึ้น นี่คือสรุปสั้นๆ ว่าเครื่องมือแต่ละตัวทำหน้าที่อะไรครับ:

### 1️⃣ **Prisma (เดอะ แบก ฐานข้อมูล)**
*   **มันคือ**: **ORM (Object-Relational Mapping)**
*   **หน้าที่**: เป็นตัวกลางที่ทำให้เราเขียนโค้ดภาษา TypeScript คุยกับฐานข้อมูล (PostgreSQL) ได้โดยตรง โดยไม่ต้องเขียนคำสั่ง SQL ที่ซับซ้อน ช่วยให้เราจัดการข้อมูลได้เหมือนการจัดการ Object ปกติในโค้ดครับ

### 2️⃣ **Express (หัวใจของ Backend)**
*   **มันคือ**: **Web Framework** สำหรับ Node.js
*   **หน้าที่**: เป็นตัวโครงสร้างที่ใช้สร้าง API ทั้งหมดที่เราเห็นครับ มันมีหน้าที่รับ Request จากหน้าบ้าน, ส่งไปให้ Route จัดการ และส่งคำตอบ (Response) กลับไป เป็นตัวเชื่อมโยงทุกอย่างในหลังบ้านเข้าด้วยกัน

### 3️⃣ **TypeScript (ผู้คุมกฎโค้ด)**
*   **มันคือ**: ภาษาระดับสูงที่ต่อยอดมาจาก **JavaScript**
*   **หน้าที่**: บังคับให้เรากำหนด "ชนิดของข้อมูล" (Type) ให้ชัดเจน เช่น ตัวแปรนี้ต้องเป็นตัวเลขเท่านั้น หรือ Object นี้ต้องมีชื่ออีเมลนะ ถ้าเราเขียนผิด โค้ดจะฟ้องทันทีตั้งแต่ตอนเขียน ช่วยป้องกัน Bug ได้มหาศาลครับ

### 4️⃣ **JWT (JSON Web Token - บัตรผ่านรหัสลับ)**
*   **มันคือ**: **มาตรฐานการจำแนกตัวตน (Authentication)**
*   **หน้าที่**: เมื่อเรา Login สำเร็จ หลังบ้านจะส่ง JWT มาให้หนึ่งใบ (เป็นชุดรหัสยาวๆ) หน้าบ้านจะเก็บบัตรนี้ไว้ และส่งแนบไปกับทุกคำขอข้อมูล (Request) เพื่อยืนยันว่า "ฉันคือตัวจริงนะ" โดยไม่ต้องส่งรหัสผ่านไปทุกครั้งครับ

### 5️⃣ **Axios (พนักงานรับส่งข้อมูล)**
*   **มันคือ**: **HTTP Client** (ตัวช่วยส่งคำขอ)
*   **หน้าที่**: เป็นห้องสมุด (Library) ในหน้าบ้านที่ใช้สำหรับ "ยิง" API ไปหาหลังบ้านครับ มันเก่งกว่าการใช้คำสั่งพื้นฐานตรงที่มันจัดการเรื่อง JSON ให้อัตโนมัติ, ใส่บัตรผ่าน (Token) ได้ง่าย และจัดการเรื่อง Error ได้เป็นระเบียบมากครับ

---

## 🎤 ส่วนที่ 12: เคล็ดลับการตอบคำถามอาจารย์ (Presentation Tips)

เพื่อให้ดูเหมือนคุณทำเองและแม่นยำใน Logic ให้ลองจำคีย์เวิร์ดเหล่านี้ไปตอบครับ:

1.  **ถ้าถามว่า "ทำไมใช้ TypeScript?"**
    *   **คำตอบ**: "เพื่อให้โค้ดมีความปลอดภัยด้านข้อมูล (Type Safety) ครับ เวลาเขียนผิดระบบจะบอกทันที ทำให้ทำงานได้เร็วและบั๊กน้อยลง"
2.  **ถ้าถามว่า "ทำไมต้องมี Middleware?"**
    *   **คำตอบ**: "เพื่อแยกหน้าที่การตรวจสอบสิทธิ์ออกจาก Logic ของแอปครับ ทำให้เราไม่ต้องเขียนโค้ดเช็ค Token ซ้ำๆ ในทุกหน้า แค่ใส่ Middleware เข้าไปก็คุมความปลอดภัยได้เลย"
3.  **ถ้าถามว่า "Prisma ดีกว่าเขียน SQL ตรงไหน?"**
    *   **คำตอบ**: "ทำให้เราจัดการฐานข้อมูลในรูปแบบ Object ได้ง่ายขึ้นครับ (ORM) และมีระบบ Migration ที่ช่วยเก็บประวัติการเปลี่ยนแปลงของโครงสร้างฐานข้อมูลอย่างเป็นระเบียบ"
4.  **ถ้าถามว่า "หน้าบ้านคุยกับหลังบ้านยังไง?"**
    *   **คำตอบ**: "ใช้ REST API และรับส่งข้อมูลด้วยรูปแบบ JSON ครับ โดยใช้ Axios เป็นตัวจัดการ Request"

---

## 📂 ส่วนที่ 13: โครงสร้างโปรเจกต์ (Project Structure)
เวลาอาจารย์ถามว่า "ไฟล์นี้อยู่ไหน?" หรือ "ทำไมแบ่งโฟลเดอร์แบบนี้?" คุณจะได้ตอบได้ทันทีครับ:
```text
/Backend
  ├── /lib          -> [prisma.tsx] ตัวเชื่อมต่อฐานข้อมูลตัวกลาง
  ├── /middleware   -> [auth.tsx] ตัวเช็คสิทธิ์ (ตำรวจ) ก่อนเข้าถึงข้อมูล
  ├── /routes       -> [auth, bookings, classes, etc.] กฎการทำงานแยกตามหมวดหมู่
  ├── server.tsx    -> จุดเริ่มต้นและศูนย์รวมของระบบหลังบ้าน
  └── schema.prisma -> พิมพ์เขียวที่กำหนดรูปร่างของตารางในฐานข้อมูล

/Frontend
  ├── /src
  │    ├── /api     -> [client.tsx] ศูนย์รวมฟังก์ชันส่งข้อมูลไปหา Backend
  │    ├── /context -> [AuthContext.tsx] ตัวจัดการสถานะการ Login ของทั้งแอป
  │    ├── /pages   -> [Home, Admin, Packages, etc.] หน้าจอที่ผู้ใช้เห็น
  │    └── types.ts -> ตัวกำหนดมาตรฐานข้อมูล (Interface)
```

---

## 📋 ส่วนที่ 14: สรุปรายการ API (API Reference)
ตารางสรุปแบบด่วน สำหรับดูภาพรวมการสื่อสารของระบบ:

| หมวดหมู่ | Method | URL | หน้าที่สำคัญ |
| :--- | :---: | :--- | :--- |
| **Auth** | POST | `/api/auth/login` | ตรวจรหัสและส่ง Token กลับ |
| **Auth** | POST | `/api/auth/register` | บันทึกผู้ใช้ใหม่ลงตาราง User |
| **Classes** | GET | `/api/classes` | ดึงข้อมูลคลาส + เทรนเนอร์มาโชว์ |
| **Bookings**| POST | `/api/bookings` | บันทึกการจอง (ใช้ Transaction) |
| **Packages**| POST | `/api/packages/subscribe` | บันทึกการซื้อแพ็กเกจสมาชิก |
| **Admin** | POST | `/api/classes` | สร้างคลาสใหม่ (ต้องเป็น Admin) |

---

## 🕸️ ส่วนที่ 15: ผังความสัมพันธ์ข้อมูล (Simplified ER Diagram)
ถ้าอาจารย์ถามเรื่องความสัมพันธ์ของข้อมูล (Relationships) ให้อธิบายตามนี้ครับ:
1.  **User ➔ MemberPackage**: "แบบ 1 ต่อ Many" (ผู้ใช้ 1 คน ซื้อหลายแพ็กเกจเพื่อเก็บประวัติ)
2.  **User ➔ Booking**: "แบบ 1 ต่อ Many" (ผู้ใช้ 1 คน จองได้หลายคลาส)
3.  **Class ➔ Booking**: "แบบ 1 ต่อ Many" (1 คลาส มีคนจองได้หลายคน)
4.  **Trainer ➔ Class**: "แบบ 1 ต่อ Many" (เทรนเนอร์ 1 คน สอนได้หลายคลาส)

---

> [!TIP]  
> **จุดแข็งของโปรเจกต์คุณ**: คือการจัดการ **Role-Based Access Control (RBAC)** ที่ชัดเจน และการใช้ **Transaction** ในการจอง ซึ่งเป็นสิ่งที่ระบบระดับมืออาชีพเค้าทำกันครับ!
