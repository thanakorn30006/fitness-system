
## สิ่งที่ต้องติดตั้งก่อน

1. **Node.js** → https://nodejs.org (ดาวโหลด LTS)
2. **PostgreSQL** → https://www.postgresql.org/download
3. **Git** → https://git-scm.com

---

## ขั้นตอนติดตั้ง

### 1. Clone โปรเจค

```bash
git clone [URL ของ repo]
cd [ชื่อโฟลเดอร์]
```

---

### 2. ตั้งค่า Database

เปิด **pgAdmin** หรือ **psql** แล้วสร้าง database ชื่อ `fitness`:

```sql
CREATE DATABASE fitness;
```

---

### 3. สร้างไฟล์ .env

ไปที่โฟลเดอร์ `Backend/` แล้วสร้างไฟล์ชื่อ `.env` (ไม่มีนามสกุล) ใส่ข้อความนี้:

```
DATABASE_URL="postgresql://postgres:รหัสผ่านของคุณ@localhost:5432/fitness"
JWT_SECRET="my-secret-key-12345"
PORT=5001
```

> ⚠️ เปลี่ยน **รหัสผ่านของคุณ** เป็น password ที่ตั้งตอนติดตั้ง PostgreSQL

---

### 4. ติดตั้ง Backend

```bash
cd Backend
npm install
npx prisma db push
npm run dev
```

> ✅ ถ้าเห็น `🚀 Backend server running on http://localhost:5001` แปลว่าสำเร็จ

---

### 5. ติดตั้ง Frontend

เปิด Terminal ใหม่อีกหน้าต่าง:

```bash
cd Frontend
npm install
npm start
```

> ✅ เบราว์เซอร์จะเปิดที่ `http://localhost:3000` อัตโนมัติ

---

